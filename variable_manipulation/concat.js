// Declare variables (string, number, boolean) and perform operations:
// Concatenate two strings.


const myFirstName = "Utsukta";//string declaration
const myLastName = "Parajuli";

const myPhoneNumber = "9810312106";
console.log(myPhoneNumber);
var a = 10;//number declaration
a = 5;//var can be overwritten
console.log(a);

let b = 6;
console.log(Boolean(b));//true or false

let c = 0;
console.log(Boolean(c));//0 is false


let d = -2;
console.log(Boolean(d));//- is true
var fullName = myFirstName.concat(myLastName);//concatenates first and last name

console.log(fullName);